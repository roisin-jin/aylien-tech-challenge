main-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 20s
  }
}

paint-ws {
  url = "http://localhost:8080/"
  url = ${?PY_APP_URL}
  endpoints {
    index = "/v1"
    crash = "/crash"
  }
}
