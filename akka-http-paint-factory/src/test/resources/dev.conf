include "application"

# default config for tests, we just import the regular conf
database {
  url = "jdbc:h2:mem:test;MODE=MYSQL;DB_CLOSE_DELAY=-1;IGNORECASE=TRUE;DATABASE_TO_UPPER=false"
  driver = "org.h2.jdbc.Driver"
  user = "rootTest"
  password = "rootest123"
  numThreads = 4
  maxConnections = 8
  minConnections = 2
  registerMbeans = true
}