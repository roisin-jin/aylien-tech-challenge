include "application"

# default config for tests, we just import the regular conf
database {
  url = "jdbc:h2:mem:nothing;MODE=MYSQL;DB_CLOSE_DELAY=-1;IGNORECASE=TRUE;DATABASE_TO_UPPER=false"
  driver = "org.h2.jdbc.Driver"
  user = "rootTest"
  password = ""
  numThreads = 4
  maxThreads = 8
  maxConnections = 8
  minConnections = 2
  registerMbeans = true

  numberOfThreads = 10
}